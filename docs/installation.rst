============
Installation
============

At the command line::

    $ easy_install cloudflare

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv cloudflare
    $ pip install cloudflare
